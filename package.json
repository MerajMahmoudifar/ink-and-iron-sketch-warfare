{
  "name": "ink-and-iron-sketch-warfare",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "npx wrangler@3 pages deploy . --project-name=ink-and-iron-sketch-warfare",
    "publish": "git add . && git commit -m \"Update project\" && git push && npm run deploy"
  }
}
